const Router = require("express");
const router = new Router();

router.post("/",);

router.get("/",);

router.get("/:id",);

router.get("/admin/all",);

router.put("/:order_id",);


module.exports = router;